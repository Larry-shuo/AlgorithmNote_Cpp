#include<bits/stdc++.h>
#include<numeric>
using namespace std;
//实现对容器元素的批量赋值
itoa();
//数组
iota(array, array+length[array], number);
//vector容器
iota(vector.begin(), vector.end(), number);//number为初值,第一个元素值


//求和
int sum = accumulate(v.begin(),v.emd(),0);//第三个参数为累计的初值

string sum = accumulate(v.begin(), v.end(), string(""));

//迭代器：
begin() 与 end() 相匹配
rbegin() 与 rend() 相匹配
反向迭代器与正向迭代器返回的不是一个类型
